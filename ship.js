export default class Ship {
	constructor(size, hitsNumber) {
		this.size = size;
		this.hitsNumber = hitsNumber;
		this.isSunk = false;
	}
	hit(hit) {
		/*this.hit*/
		/*for(let i= 0; i<hit.length; i++) {
			this.hit = hit+1;
		}*/
	}
	isSunk(shipLength) {
/*		this.isSunk = false;
		if(shipLength == Ship.length) return true;*/
	}
}


